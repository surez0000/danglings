/// <reference types="vite/client" />

/* Injected by vite.config.ts from package.json; the Tauri build reads the real
   version from the app bundle and this is only the plain-browser fallback. */
declare const __APP_VERSION__: string;
